{
  "name": "server",
  "private": true,
  "scripts": {
    "dev": "cargo run --bin server",
    "build": "cargo build --bin server",
    "lint": "cargo clippy --all-targets -- -D warnings",
    "format": "cargo fmt",
    "check": "cargo fmt --check && cargo check",
    "test": "cargo test"
  }
}
